# UIKitExample

一个使用 XcodeGen 生成的 UIKit 示例工程。

## 生成工程

```bash
xcodegen generate
```

## 构建

```bash
xcodebuild -project UIKitExample.xcodeproj -scheme UIKitExample -destination 'generic/platform=iOS Simulator' build
```
